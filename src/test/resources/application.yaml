server:
  port: 13377

spring:
  application:
      name: notification-service
  datasource:    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
security:
  zsign:
    secret: local-deploy
    salt: local-deploy